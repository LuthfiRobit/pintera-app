---
paths:
  - 'app/Enums/**'
---

# Enums

## app/Enums root is a frozen legacy zone
Do not add new enum files to app/Enums/ (root) — it holds pre-domain-refactor enums only. New enums go in app/Domains/{Domain}/Enums/.
