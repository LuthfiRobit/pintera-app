---
paths:
  - 'app/Domains/*/Enums/**'
---

# Domains Enums

## New enums are backed string enums, in the domain
Enums are backed string enums (`: string`), not pure enums. New enums live in app/Domains/{Domain}/Enums/, not app/Enums/ root.
